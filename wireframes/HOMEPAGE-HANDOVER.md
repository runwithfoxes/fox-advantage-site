# Homepage build - handover brief

**For:** a fresh Claude Code terminal working WITH Paul on the runwithfoxes.com homepage.
**Working dir:** `~/projects/fox-advantage-site-repo`
**Status:** the *product-page format* and the first product (AI Ad resizer) are LOCKED in `wireframes/`. Your job is the **homepage** - the storefront grid of product cards that links to those product pages. **Nothing goes live until Paul says so.**

There is another terminal open (the one that designed all of this). **Ask it questions inline** - see "The QA channel" below. Do that instead of guessing.

---

## 1. The reframe (read this first, then the CONTEXT file)

RWF sells **products** - each one a buyer-named system ("AI Ad resizer", "segmentation AI", "email writer") that we **build around a client's brand and hand over** (not self-serve). "Products" is the buyer-facing label; under the hood they are still modules of the campaign manager. Inspiration: **Every's Product Studio** (a card-grid storefront) - borrow the card presentation only.

**Navigation (like Every):** the homepage stays a **storefront grid of product cards**; clicking a card opens that product's **own page** (the homepage itself stays intact - no inline expanding).

**Read before touching anything:**
- `~/paul-hub/clients/rwf/CONTEXT.md` - the top entry "24 Jun (evening)" has the full reframe + the locked format.
- `~/projects/fox-advantage-site-repo/CLAUDE.md` - **deploy guardrails** (deploys from `main`; do NOT touch the nav, hero video, Isa chatbot, or bottom bar; the half-built `bench-page` branch).

## 2. What exists already (don't rebuild these)

- **The storefront wireframe to build from:** `wireframes/homepage-blueprint-storefront.html` (full-page wireframe: existing homepage chrome as low-fi boxes + the modules section rebuilt as a **card grid**; each card links to a product page). The rejected alternative was a left rail: `wireframes/homepage-blueprint-menu.html`.
- **The locked product page** (what cards link to): `wireframes/module-ad-maker.html` (the AI Ad resizer). **Do not edit it.** New product pages are generated by the **`/product-page` skill**, not by hand.
- **The live homepage component:** `src/components/HomePage.tsx` - the "Where can AI be built into marketing?" section (currently a 7-row accordion) is what the storefront grid will eventually replace.

## 3. Your job

Work the **homepage storefront** with Paul: refine the card grid in the wireframe, settle the product list, then (only when Paul approves) port it into `src/components/HomePage.tsx`. The page chrome above and below the modules section (hero, bio, Substack carousel, testimonials, book block) stays as-is.

## 4. Brand + execution rules (law)

- Brand: `/branded-page` spec + `~/.claude/skills/branded-page/flow-diagram-aesthetic.md`. Logo `/Run​withfoxes`, sharp corners, no nav border, tokens/fonts as specced.
- Voice: `/voice` on every visible line. No em dashes, sentence case.
- **One text width per page** - never mix a full-width line with a half-width (ch-capped) one.
- **Screenshot and LOOK before you call any layout done** (headless Chrome -> read the PNG). Reasoning about CSS in your head is not enough.

## 5. The QA channel - ask the other terminal inline

The other terminal is watching `wireframes/HOMEPAGE-QA.md`. To ask it something:

1. **Append** a block to `wireframes/HOMEPAGE-QA.md`:
   ```
   ### Q<n> (short-label): your question, with enough context to answer cold.
   ```
   Save the file. No commit needed (same machine).
2. **Poll** the file every ~30s for an inline reply: `**A (short-label):** ...`.
3. If no answer in ~5 minutes, make the call that fits the doctrine above, mark it `**ASSUMED:** ...`, and carry on.
4. Paul's mid-flight steers may appear as `### DIRECTIVE (Paul): ...` - treat as priority.

## 6. Likely open questions (ask the channel, don't assume)

- Does "products" replace "modules" in the storefront wording (intro copy, labels)?
- The product menu: which products, the exact buyer-names, and the order. Settle this before building cards.
- Each card needs a small "visual of it working" - real approved asset or a clean coded mock?
- Does the storefront grid REPLACE the homepage accordion section, or sit above it?
- The campaign-manager "bundle" card: peer in the grid, or set apart as the operation that runs the others?
